# Truffle
1. Copy `truffle/dist.env` to `truffle/.env`
2. Open `.env` edit params to correct value

```
truffle build
truffle compile
truffle migrate
truffle deploy
```

# React
```
yarn install
yarn start
```
